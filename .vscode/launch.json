{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "All MCP Servers (F5)",
			"type": "node-terminal",
			"request": "launch",
			"command": "${workspaceFolder}/src/start_mcp_servers.sh",
			"cwd": "${workspaceFolder}",
			"consoleName": "MCP Servers"
		}
	]
}
